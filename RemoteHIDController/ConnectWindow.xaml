<Window x:Class="RemoteHIDController.ConnectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Connect to ESP32 HID Device" 
        Height="200" 
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Enter ESP32 Device IP Address:" 
                   FontSize="14" 
                   Margin="0,0,0,10"/>

        <TextBox Grid.Row="1" 
                 x:Name="IpAddressTextBox"
                 FontSize="14" 
                 Padding="5"
                 Text="192.168.1.177"/>

        <TextBlock Grid.Row="2" 
                   x:Name="StatusTextBlock"
                   Foreground="Red"
                   Margin="0,10,0,0"
                   TextWrapping="Wrap"/>

        <Button Grid.Row="4" 
                x:Name="ConnectButton"
                Content="Connect" 
                Height="35" 
                FontSize="14"
                Click="ConnectButton_Click"
                IsDefault="True"/>
    </Grid>
</Window>
