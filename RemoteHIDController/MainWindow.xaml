<Window x:Class="RemoteHIDController.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RemoteHIDController"
        mc:Ignorable="d"
        Title="Remote HID Controller" 
        Height="600" 
        Width="800"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
        Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Status Bar -->
        <Border Grid.Row="0" 
                Background="#2C3E50" 
                Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0"
                           x:Name="StatusTextBlock"
                           Foreground="White"
                           FontSize="14"
                           Text="Not Connected"/>
                
                <Button Grid.Column="1"
                        x:Name="DisconnectButton"
                        Content="Disconnect"
                        Padding="10,5"
                        Click="DisconnectButton_Click"
                        IsEnabled="False"/>
            </Grid>
        </Border>

        <!-- Capture Area -->
        <Border Grid.Row="1" 
                x:Name="CaptureArea"
                Background="#34495E"
                Cursor="Cross"
                Focusable="True"
                MouseDown="CaptureArea_MouseDown"
                MouseUp="CaptureArea_MouseUp"
                MouseMove="CaptureArea_MouseMove"
                MouseWheel="CaptureArea_MouseWheel">
            <Viewbox>
                <TextBlock x:Name="CaptureTextBlock"
                           Foreground="#95A5A6"
                           FontSize="24"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           TextAlignment="Center"
                           Text="Click here to start capturing mouse and keyboard&#x0a;&#x0a;Press ESC to stop capturing"/>
            </Viewbox>
        </Border>
    </Grid>
</Window>
